# Committed on purpose so this reproduction runs straight after a clone.
# Throwaway values for a demo app, no secrets here.

TZ=UTC
PORT=3333
HOST=localhost
NODE_ENV=development

LOG_LEVEL=info
APP_KEY=reproduction_only_not_a_real_secret
APP_URL=http://${HOST}:${PORT}

SESSION_DRIVER=cookie
